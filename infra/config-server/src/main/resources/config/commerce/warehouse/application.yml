server:
  port: 0

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:54357/warehouseDB
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update